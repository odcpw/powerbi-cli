# Post-pass simulation

The same plausible wrong commands return exact canonical discovery commands. The skill routes visual deletion through the CLI and explains hierarchy drill versus multi-series comparison. Duplicate Desktop titles resolve to the launched/new process or fail as ambiguous.
