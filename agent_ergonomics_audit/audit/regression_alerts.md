# Regression Alerts

No regressions detected in the focused suite. Watch these boundaries in future passes:

- A focused capabilities response should remain below 20 KB for `--for validate`.
- Same-title baseline processes must never outrank the association/new process.
- Descendant foreground verification must not broaden to unrelated processes.
- Desktop window and screenshot proof must continue to disclaim canvas/refresh compatibility.
