# R-010 Between Slicer

- Direct visual creation emits the exact `'Between'` PBIR literal for an integer year.
- Dashboard-spec validation accepts numeric/date range fields and rejects text fields.
- `report visuals set-bindings` preserves the same type invariant for an existing Between slicer.
