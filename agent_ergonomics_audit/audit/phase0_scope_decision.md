# Pass 1 Scope Decision

Mode: full, focused on friction directly observed while repairing a real PBIP dashboard.

Included surfaces:

- Desktop open-check and screenshot target selection with duplicate report titles.
- Screenshot foreground-ownership verification for Power BI child UI processes.
- Focused capability discovery payload size.
- Recovery from plausible but misplaced command paths.
- The in-repo Power BI dashboard repair and regression skill.
- MCP child-process monitoring performance, added after the full suite exposed a
  reproducible 13-second cancellation path.

Explicit constraints: keep the change small, preserve Windows/Linux/macOS CLI behavior, add no compatibility aliases, and do not attempt speculative canvas/refresh automation.

Direct dashboard transcripts supplied the pre-change evidence, so CASS mining and synthetic external triangulation were unnecessary. The current system policy also disallowed spawning a new review agent; fresh-eyes review is recorded as a separate local pass.
