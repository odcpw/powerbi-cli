# R-011 Canonical Dashboard Loop

- Skill validation passes.
- The operating loop uses one canonical project and reusable QA output.
- Interactive Desktop sessions require `desktop open` / `desktop close`; raw launches and `--leave-open` are rejected as workflow patterns.
